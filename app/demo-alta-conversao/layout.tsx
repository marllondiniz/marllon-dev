export const metadata = {
  title: "Demo — Alta conversão | zinid.tech",
};

export default function DemoAltaConversaoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
